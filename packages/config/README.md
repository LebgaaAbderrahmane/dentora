# @dentora/config

Shared tooling configuration for the Dentora monorepo.

Planned contents (implemented in Phase 0.2):

- `tsconfig.base.json` — shared TypeScript base config extended by all workspaces
- `oxlint` / Prettier presets
- Tailwind design tokens presets
