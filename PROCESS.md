# Dentora PMS — Process & Roadmap Log

> Living document: updated at every session. Keeps decisions, step-by-step plan, progress, and conventions in one place so we're always in context.
> **Rules:** update §14 (session log) and flip checkbox statuses in §9 whenever you make progress. Documentation lives in `README.md` + `docs/` (see §11).

---

## 1. Vision

Transform `dentora` from a static marketing site into a complete, self-hosted dental practice management system ("Dentora PMS") for a single Algerian clinic (designed multi-branch ready):

- **Patients** — records, medical history, odontogram, documents/X-rays
- **Appointments** — calendar, statuses, waiting list, no-shows, reminders
- **Billing & Finance** — invoices, payments, receipts, expenses, daily cash close-out, P&L
- **Stock** — products, suppliers, purchase orders, ledger, expiry alerts, per-treatment consumption, sterilization logs
- **Staff & Interns** — roles/permissions, schedules, attendance, intern hours/mentor/rotation, payroll
- **Dashboard + Reports** — KPIs, occupancy, revenue, stock valuation, CSV/PDF export
- **Patient portal** — login, history/invoices, online booking/cancel
- **Public site** — stays separate; booking modal creates pending requests

Requirements: visually & code professional, fr/ar/en (RTL), DZD currency. Medical data ⇒ encryption at rest, audit logging, and access control from day one. Production system → will be operated and extended by other people, so documentation is a first-class deliverable.

## 2. Current state (baseline commit `5323fcc`)

- Static Vite React 19 SPA at repo root (marketing site for "Dentora").
- Tailwind 4, i18next (fr/ar/en), motion, lucide, shadcn-style UI.
- No backend, no DB, no auth. Booking = WhatsApp modal.
- Lint: oxlint. Build: `tsc -b && vite build`. Package manager: pnpm.

**After 0.1:** the SPA now lives in `apps/web/` inside a pnpm monorepo (`apps/{web,admin,portal,api}`, `packages/{contracts,ui,config}`).

## 3. Confirmed decisions

| Area           | Decision                                                                                                                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Deployment     | Self-hosted web app, VPS + Docker Compose; **manual deploy runbook initially** (ADR 011), CI/CD decided in 6.6                                                            |
| Backend        | Express + TypeScript + Prisma + PostgreSQL                                                                                                                                |
| API style      | REST + Zod; schemas shared via `packages/contracts` (ADR 002)                                                                                                             |
| Frontend       | **Three** Vite SPAs: `web` (public), `admin` (staff), `portal` (patients) — ADR 003                                                                                       |
| Frontend stack | React Router v7, TanStack Query, React Hook Form + Zod, Zustand, recharts, Tailwind 4                                                                                     |
| Auth           | httpOnly cookie sessions + server-side session store; revoke-all endpoint; revocation on role change                                                                      |
| Roles          | `admin` · `dentist` · `receptionist` · `accountant` · `intern` · `patient`                                                                                                |
| Scope          | Staff app + patient portal; public marketing site kept separate                                                                                                           |
| Branches       | Single clinic now, `branchId` everywhere (multi-branch ready)                                                                                                             |
| Offline        | PWA (Workbox): cache app shell, offline-queue bookings                                                                                                                    |
| i18n           | fr / ar / en, RTL, DZD (reuse existing i18next system)                                                                                                                    |
| Monorepo       | pnpm workspaces: `apps/{web,admin,portal,api}`, `packages/{contracts,ui,config}`                                                                                          |
| Object storage | **MinIO** (S3-compatible, self-hosted) for documents/X-rays; **proxied download through API** (RBAC + audit per view); client-side envelope encryption (ADR 005, amended) |
| Encryption     | Field-level AES-256-GCM for MedicalHistory; app-level key via env (ADR 006)                                                                                               |
| Audit log      | Basic (view/edit on patient records) from Phase 0; admin UI in Phase 6 (ADR 007)                                                                                          |
| Observability  | Sentry on api + admin/portal from Phase 0 (ADR 009)                                                                                                                       |
| Backups        | Nightly pg_dump + WAL archiving/PITR; documented RTO/RPO; restore runbook (ADR 010)                                                                                       |
| Infra          | postgres + minio + api + SPAs (nginx) + caddy TLS                                                                                                                         |

## 4. Architecture

```
dentora/
├─ apps/
│  ├─ web/          # public marketing site (moved from root; booking → API)
│  ├─ admin/        # staff SPA
│  ├─ portal/       # patient SPA
│  └─ api/          # Express server (REST + Zod)
├─ packages/
│  ├─ contracts/    # Zod schemas + inferred types (api ↔ frontends)
│  ├─ ui/           # design system (grown incrementally, not spec-built)
│  └─ config/       # shared tsconfig / lint presets
├─ infra/           # docker-compose, nginx/caddy, backup scripts, runbook
├─ docs/            # human documentation (see §11)
├─ README.md        # project overview + quickstart
└─ PROCESS.md       # this file
```

Data flow: frontends → api (Zod-validated REST) → PostgreSQL + MinIO. Files are never directly reachable; the API proxies every upload/download through RBAC + audit.

## 5. Infrastructure (VPS + Docker)

- Services: `postgres` (volume + WAL archiving) · `minio` (volume) · `api` · three SPAs behind `nginx` · `caddy` reverse proxy (auto Let's Encrypt).
- Backups: nightly `pg_dump` + continuous WAL archive for point-in-time recovery; retention 7/30/90; restore script + runbook with measured RTO/RPO.
- Observability: Sentry; backend structured request logging (pino).
- Security: non-root containers, helmet, rate limiting, CSRF, Zod validation everywhere, audit log, field-level encryption.

## 6. Data model (Prisma — grown per feature, branch-scoped)

**Phase 0 (minimal):** Users · Roles · Sessions · Patients (skeleton) · Branch.
**Grown in later phases:** Permissions matrix · MedicalHistory (encrypted) · Odontogram/Tooth · Documents (MinIO key refs) · Services & Pricing · Appointments (+WaitingList, NoShow) · Treatments (per-tooth/service) · Invoices/InvoiceLines · Payments (CASH/CHEQUE/CARD/TRANSFER) · Expenses · Payroll · Staff & Interns · Attendance · Shifts · Products · StockMovement (ledger) · ProductBatch (expiry) · Suppliers · PurchaseOrders · TreatmentStockUsage · SterilizationLog · Notifications/Reminders · AuditLog · Settings.

## 7. Standards

### 7.1 Code standards (professional bar)

- TypeScript strict; shared lint (oxlint) + Prettier; conventional commits.
- Every feature: Zod schema in `packages/contracts` → inferred types; tested (Vitest units; Playwright e2e for core flows).
- Sensitive fields must use the encryption helper (never plaintext in DB).
- Every patient-record read/mutation writes an audit event.
- No secrets in code; `.env` via `infra/.env.example`; i18n keys never hardcoded in components.

### 7.2 Git & documentation rules

- **Commit after every roadmap step** (§9) with a conventional message scoped to the step, e.g. `feat(api): auth + RBAC middleware`.
- **Phase or major step finished & DoD green → push to a dedicated branch**: `feat/<phase>-<slug>` or `fix/<slug>`.
- Merge to `main` only when DoD is met; `main` always stays deployable; tag releases for production.
- Docs are written in the **same commit** as the code they describe — never a separate cleanup task.
- Update §14 log + flip §9 checkboxes in the same commit.

## 8. Definition of Done (every step)

- [ ] Lint-clean (`pnpm lint`) + typecheck + build pass
- [ ] Tests added & passing (Vitest; Playwright where applicable)
- [ ] Prisma migration created when schema changes
- [ ] Audit events covered for any patient-data access/change
- [ ] i18n fr/ar/en complete + RTL checked
- [ ] Docs updated (`docs/`) + conventional commit + §9/§14 statuses flipped

## 9. Roadmap — step-by-step

Legend: `[ ]` todo · `[/]` in progress · `[x]` done

### Phase 0 — Walking skeleton (thin vertical slice)

Goal: prove login → authed API → admin dashboard stub end-to-end. No speculative building.

- [x] **0.0 Planning** — decisions locked (ADRs 003/005–007/009–011), PROCESS.md written
- [x] **0.1 Monorepo restructure** — move web app to `apps/web/`; scaffold `apps/{admin,portal,api}` + `packages/{contracts,ui,config}`; root scripts (`dev/build/lint/typecheck/test`); **verify `web` builds identically** ✅
- [x] **0.2 Tooling + CI + docs skeleton** — shared tsconfig, oxlint + Prettier, Vitest, GitHub Actions (lint→typecheck→test→build); `README.md` + `docs/` tree + ADR files; deploy stays manual (0.9 runbook) ✅
- [x] **0.3 Docker + DB + storage + proxy** — docker-compose (postgres + minio + caddy + nginx), `infra/.env.example`, volumes, WAL archiving on ✅
- [x] **0.4 Minimal Prisma + encryption lib** — Prisma 7.9 driver-adapter schema (Branch/User/Session/Patient/Setting + Role enum), migration applied, seed creates admin; AES-256-GCM helper with tests; `/api/system/status`; api bundled ESM with external deps, verified standalone + in Docker ✅
- [x] **0.5 Auth + RBAC + sessions** — opaque 256-bit token cookie sessions (SHA-256 in DB), login/logout/me/revoke-all/change-password, role/ADMIN middlewares, **role change revokes all sessions**; admin login page + ADMIN users panel ✅
- [x] **0.6 Basic audit log** — `AuditLog` table (action/target/metadata/ip/userAgent), service + middleware helper, wired into auth + user events; `GET /api/audit` (ADMIN, paginated/filtered); admin panel; patient events ready for 1.1 ✅
- [x] **0.7 Sentry** — DSN-guarded on api + admin/portal (build-time `VITE_SENTRY_DSN`); API error middleware captures with user context; pino structured logs alongside (ADR 009) ✅
- [x] **0.8 App shells** — admin layout + login-gated dashboard stub reading real data; portal skeleton; i18n fr/ar/en + theme + RTL; minimal ui tokens/Button/Input/Card/Toast only ✅
- [x] **0.9 Backups + deploy runbook** — WAL archiving fixed (volume ownership), `backup.sh`/`restore.sh`, PITR drill passed (RTO ~2 s, RPO ≤5 min), `docs/backup-restore.md` + `docs/deploy.md` ✅
- **DoD:** admin logs in → dashboard stub renders live data; `web` untouched & building.

### Phase 1 — Clinical core (features drive the design system)

- [x] **1.1 Patients CRUD + search/pagination** — branch-scoped routes (ADMIN/DENTIST/RECEPTIONIST), create/update/detail/list with ILIKE search (`q`) + `archived` filter, soft-delete via `archivedAt` (+`PATIENT_ARCHIVED`/`PATIENT_RESTORE` audit events), `notes` AES-256-GCM encrypted at rest and decrypted only on detail; API contract tests + live-verified; admin PatientsView (search, table, create/edit/detail modals, archive/restore) ✅
- [x] **1.2 Medical history (record/edit, encrypted blob)** — `PatientMedicalHistory` 1:1 per patient, whole-record AES-256-GCM blob; `GET/PUT /api/patients/:id/medical-history`; **optimistic-lock** (`version` plaintext column, atomic `updateMany` where patientId+version) returning `VERSION_CONFLICT` 409 with current version; PUT is upsert (first save creates); `PATIENT_MEDICAL_VIEW`/`UPDATE` audit; admin detail tab with 7 free-text fields ✅
- [x] **1.3 Odontogram (interactive tooth chart)** — `PatientOdontogram` 1:1 encrypted JSON blob (FDI codes 11–48, per-surface condition enums); `GET/PUT /api/patients/:id/odontogram` same version-lock + upsert + audit pattern; hand-rolled SVG chart (32 teeth, 4 quadrants) with ≥44px touch targets in a per-tooth editor, keyboard + aria support; admin detail tab ✅
- [x] **1.4 Documents upload/view (MinIO, envelope encryption, proxied download, audit on view)** — `PatientDocument` (objectKey `branch/patient/document`, originalName, mimeType, `size` = plaintext byte length); per-object envelope AES-256-GCM keys stored as object metadata (ADR 005 amended); upload raw ≤50 MB, proxied download streams decrypted with original `size`; `PATIENT_DOCUMENT_CREATE`/`VIEW` audit; admin DocumentsTab with progress ✅
- [x] **1.5 Appointments calendar (day/week/month) + conflict check + statuses** — FullCalendar 6.1.21 (exact-pinned core/react/daygrid/timegrid/interaction), CSS extracted from npm global bundle (v6 ships no .css) + themed; statuses PENDING/CONFIRMED/COMPLETED/CANCELLED/NOSHOW; dentist+branch validation; dentist AND patient conflict checks excluding terminal statuses; encrypted notes (list never returns them, detail decrypts); 6 audit actions; admin calendar with create/edit/drag/resize + conflict toasts ✅
- [x] **1.6 Waiting list + no-show handling** — status-driven `WaitlistEntry` (PENDING→CONTACTED→BOOKED, terminal CANCELLED/EXPIRED), no hard delete; one active entry per patient (409 `WAITLIST_ALREADY_ACTIVE`); `BOOKED` requires a branch-scoped appointment of the same patient; notes encrypted (ADR 006); `WAITLIST_CREATE/UPDATE/BOOK/CANCEL` audit targeting the patient; derived `noShowCount`/`noShowRate` (noShow/[noShow+completed]) on patient detail; branch-scoped `/api/staff/dentists` for non-admin scheduling UIs (fixes dentist dropdown 403); admin WaitlistView (search/status filter, add dialog, contact/book/remove/expire actions, booked-from-waitlist creates a real appointment with conflict handling); patient-detail no-show line ✓
- [x] **1.7 Dashboard KPIs (today's visits, no-shows, waitlist, patients)** — `GET /api/dashboard/kpis` (ADMIN/DENTIST/RECEPTIONIST, branch-scoped, all figures derived on read, never stored); client-supplied absolute-instant windows (`from`/`to`/`windowStart` — local today + 30-day lookback, same convention as the calendar range calls so the API stays timezone-agnostic); today's visits `total` + `byStatus` + upcoming blocking-status list (capped 10); no-show today + trailing-30-day `rate30d` reusing the ADR 014 `noShow/(noShow+completed)` formula; waitlist `active` (PENDING+CONTACTED); patients non-archived `total` + `new30d`; **revenue and low-stock alerts intentionally deferred to Phases 2/3** (no invoicing/inventory domains exist; ADR 015, no placeholder data); admin dashboard rewritten from the 0.8 system-status stub → KPI cards (visits/no-shows/waitlist/patients with per-status + 30-day subtitles), today's schedule list with status badges, system-status pill + uptime + refresh button; i18n fr/ar/en; contract suites +7 (49) + `lib/dashboard` unit tests (startOfDay/addDays/statusCounts); ✓
- [x] **1.8 Public-site booking → API creates pending request (web ↔ api)** — `POST /api/public/bookings` (unauthenticated, `PUBLIC_BRANCH_ID` else first branch); find-or-create patient by phone; creates a `PENDING` waitlist entry (never an appointment — walked into the existing contact→book lifecycle, ADR 016); repeat request for the same phone answers 409 `WAITLIST_ALREADY_ACTIVE`; optional service/preferredDate/message folded into the encrypted `notes`; in-memory per-IP limiter (5/h → 429); audit `WAITLIST_CREATE` with `metadata.source: 'web'` + `createdById = null` → waitlist list exposes `source: 'staff' | 'web'` and the admin board shows a web badge; web BookingModal submits via same-origin `/api` (nginx prod, Vite dev proxy) with submitting/error/duplicate(done already handled)/success states + i18n fr/ar/en; contracts `publicBookingSchema`/`publicBookingResponseSchema` ✓
- **DoD:** end-to-end patient → appointment → dashboard flow works.

### Phase 2 — Billing & Finance

- [x] **2.1 Service catalog & pricing (DZD, duration, insurance coverage)** — `Service` model (branchId, name, category enum, `priceDZD` whole-dinar Int — no sub-unit precision, `durationMinutes`, `reimbursablePct` 0–100, archivedAt, ADR 017), migration `20260816193553_add_service_catalog`; contracts `serviceCategorySchema` + `SERVICE_CATEGORIES`, `serviceSchema` / input (defaults pct 0) / update / list {items,total} / query (q, category, archived exclude|only, limit ≤200); `/api/services` CRUD — read for the clinical trio, **write ADMIN-only** (write routes stack `requireRole('ADMIN')`), archive/restore vs hard delete, no per-read audit (catalog ≠ PHI) but `SERVICE_CREATE/UPDATE/ARCHIVE/RESTORE` audited (`AuditTarget.SERVICE`, metadata name+price); admin Catalogue view (nav item gated to the clinical trio — read-only table for DENTIST/RECEPTIONIST, create/edit/archive/restore controls render only for ADMIN), search + category filter + active/archived filter, DA price + min duration + coverage badges, create/edit modal (ADMIN); i18n fr/ar/en `catalog.*`; **2.2 clause: invoice lines must snapshot price at booking time** — catalog changes never rewrite history ✓
- [x] **2.2 Invoices + lines + numbering, status (paid/partial/unpaid/void)** — `Invoice` (branchId, patientId Restrict, `invoiceNumber` Int **allocated atomically per branch** via the settings `invoiceCounter` row in its own tx + `@@unique([branchId, invoiceNumber])` backstop, issuedAt, `voidedAt` the only stored status row, createdById) + `InvoiceLine` (**immutable snapshot**: serviceId? reference-only, serviceName, priceDZD whole-dinar, quantity; catalog changes never rewrite history — ADR 017/018); no PATCH — corrections are **void + re-issue** (ADR 018); totals derived on read (`Σ price×qty`), `status` derived by `invoiceStatus()` (`UNPAID|PARTIAL|PAID|VOID` from paidDZD/subtotalDZD/voidedAt; pre-2.3 paid=0 ⇒ UNPAID, `PAID/PARTIAL` tabled for 2.3 payments, list filter uses `voidedAt` now — later a paid-vs-total join); `/api/invoices` — read for clinical trio (+ACCOUNTANT), **create/void `requireRole('ADMIN','RECEPTIONIST')`**, list (`q` patient name or number + `status` + `patientId`), detail (lines), void (`ALREADY_VOID` 400), `UNKNOWN_PATIENT`/`UNKNOWN_SERVICE` 400, audits `INVOICE_CREATE`/`INVOICE_VOID` (AuditTarget.INVOICE, metadata number+patient+total), migration `20260817044250_add_invoices`; contracts `invoiceStatusSchema`/`INVOICE_STATUSES`, invoiceSchema (status + totals), detail (+lines), list, query, `invoiceCreateSchema` (patientId + 1–50 lines with snapshot name/price/qty≤999); admin Factures view (nav gated ADMIN/RECEPTIONIST/ACCOUNTANT, **create/void controls ADMIN+RECEPTIONIST** matching API RBAC, read-only for ACCOUNTANT), list w/ status badges + #number, create modal (patient select + line items quick-added from the catalog with snapshot name/price prefilled + qty), row-click detail (lines + totals), void action; i18n fr/ar/en `invoices.*` + audit labels; `lib/invoice.ts` (`nextInvoiceNumber`, prisma-touching) + pure `lib/invoiceStatus.ts` (`invoiceStatus` — no prisma import, CI-testable) +5 unit tests (27 api) ✓
- [x] **2.3 Payments (cash/cheque/card/transfer), partial, receipts, refunds** — `Payment` model (**one table, `kind` RECEIPT|REFUND discriminator**; refunds are immutable money-out rows bound to the receipt they reverse via `refundsId` self-relation, ADR 019); methods `CASH|CHEQUE|CARD|TRANSFER` + reference (cheque/card/transfer trace) + `receivedAt`; **`paidDZD` always derived** = Σ(RECEIPT) − Σ(REFUND) via a single `groupBy` — never stored (ADR 014/019); contracts `paymentMethodSchema`/`PAYMENT_METHODS`, `paymentKindSchema`, paymentSchema, list/query (invoiceId | invoiceNumber, limit ≤200), `paymentCreateSchema`, `MAX_PAYMENT_AMOUNT_DZD`, `refundCreateSchema`, invoiceDetailSchema += paidDZD/balanceDZD/payments; `/api/payments` — read trio+ACCOUNTANT, **create/refund `requireRole('ADMIN','RECEPTIONIST')`**; POST / (no payment on a voided invoice → 400 `INVOICE_VOIDED`, no overpayment → 400 `PAYMENT_EXCEEDS_BALANCE`, 201 + `PAYMENT_CREATE` audit w/ number+amount+method+balance), POST /:id/refund (receipt-only, bounded by its remaining net → 400 `REFUND_EXCEEDS_RECEIPT`, 201 + `PAYMENT_REFUND` audit w/ refundsPaymentId); **invoices become paid-aware**: list attaches `paidDZD` to every row (status badges now real), `UNPAID/PARTIAL/PAID` filters derived in-memory over branch candidates (single-clinic volume), `VOID` stays a voidedAt scan, detail returns payments + paidDZD + balanceDZD, **void blocked while money is collected** → 400 `INVOICE_HAS_PAYMENTS` (refund first, then re-issue — completes ADR 018's correction flow); `lib/paymentMath.ts` pure (netPaid/refundableRemaining/balanceDue — no prisma import, CI-safe) + `lib/payments.ts` (paidForInvoices/paymentNetFor/refundedForPayment/toPayment) +3 unit tests (30 api); admin InvoicesView grows a payments ledger in the invoice detail (paid/total/balance, method+date+amount+reference rows, refund rows shown as reductions, "Encaisser" modal prefilled with balance + method select cash/cheque/card/transfer, refund modal bounded by remaining), void hidden for an invoice with a balance implicitly via `INVOICE_HAS_PAYMENTS` toast, AuditView labels paymentCreate/paymentRefund, i18n fr/ar/en `payments.*` + `invoices.paid/balance`; migration `20260817052640_add_payments` ✓
- [x] **2.4 Expenses + categories** — `ExpenseCategory` fixed enum (`SALARY|RENT|SUPPLIES|EQUIPMENT|UTILITIES|MAINTENANCE|MARKETING|TAXES|OTHER`), `Expense` (branchId, category, `amountDZD` whole-dinar Int, description ≤300, `incurredAt` = the date the expense belongs to — enables 2.5 close-out/P&L sums, `voidedAt`? soft-void — **never hard-deleted**, createdById "ExpenseCreator", branchId-first indexes: incurredAt / category+incurredAt / voidedAt), migration `20260817081526_add_expenses`; AuditAction += `EXPENSE_CREATE/UPDATE/VOID`, AuditTarget += EXPENSE; contracts `expenseCategorySchema` + `EXPENSE_CATEGORIES`, expenseSchema, expenseInputSchema, expenseUpdateSchema (≥1 field), expenseListSchema {items,total}, expenseQuerySchema (q, category, from/to ISO, voided exclude|only, limit ≤200) +6 suites (78 total); `/api/expenses` — **ADMIN + ACCOUNTANT only, read AND write** (clinical roles excluded from costs, ADR 020); GET / (filters, non-voided by default), GET /:id, POST / 201 (`EXPENSE_CREATE` audit w/ amount+category+date+description), PATCH /:id (audits `EXPENSE_UPDATE` with before/after metadata, 400 `ALREADY_VOID` — a voided expense is frozen), POST /:id/void (soft, 400 `ALREADY_VOID`, `EXPENSE_VOID` audit), **no DELETE**; pure `lib/expenseMath.ts` (expenseSums by category / expenseTotal — no prisma import, CI-safe, feeds 2.5 P&L) +2 unit tests (32 api); admin ExpensesView — nav item (Wallet) gated ADMIN+ACCOUNTANT (both can write, matches API RBAC), search + category + active/voided + from/to filters, table (date, category badge, description, DA amount, voided rows dimmed), create/edit modal (category/amount/description/date) + row void; api client +5 expense methods; AuditView labels expenseCreate/update/void; i18n fr/ar/en `expenses.*` + cat labels + audit labels. **Docs**: api README expenses section, ADR 0020 + index row, §9 (2.4 [x]), §13 row, §14. **Verified**: format/lint/typecheck 8/8/test (78+32)/build 8/8; live smoke — create/list/filter/from-to windows, create→PATCH (audited before/after), void (re-void 400), RBAC (ACCOUNTANT+ADMIN 200/201, DENTIST/RECEPTIONIST 403), audit EXPENSE_* rows; smoke rows cleaned. Branch `feat/phase2-billing-24`.
- [ ] 2.5 Daily cash close-out + P&L report
- **DoD:** invoice → payment → receipt works; P&L reflects data.

### Phase 3 — Stock & Inventory

- [ ] 3.1 Products, categories, units, reorder levels
- [ ] 3.2 Suppliers + purchase orders
- [ ] 3.3 Stock ledger (in/out/adjust), batch + expiry tracking
- [ ] 3.4 Low-stock + expiry alerts
- [ ] 3.5 Treatment-stock consumption + sterilization logs
- **DoD:** stock flows with every treatment and purchase; alerts fire.

### Phase 4 — Staff & Interns

- [ ] 4.1 Staff management + schedules
- [ ] 4.2 Attendance tracking
- [ ] 4.3 Intern management (school, hours, mentor, rotation)
- [ ] 4.4 Payroll (base, bonus, deductions, net)
- **DoD:** staff/intern hours flow into payroll.

### Phase 5 — Patient portal + notifications

- [ ] 5.1 Portal login, view history/invoices, online booking/cancel
- [ ] 5.2 Notifications: WhatsApp + email reminders (configurable provider)
- **DoD:** patient books/cancels online; reminders scheduled.

### Phase 6 — Reports & hardening

- [ ] 6.1 Reports: occupancy, revenue, stock valuation + CSV/PDF export
- [ ] 6.2 Audit log admin UI + retention policy
- [ ] 6.3 PWA offline (app shell + offline booking queue)
- [ ] 6.4 Playwright e2e for core flows
- [ ] 6.5 Security/perf audit, DR drill (restore from backup verified), review Sentry dashboards
- [ ] 6.6 Final CD automation decision; README + docs consolidation
- **DoD:** full system hardened, documented, exportable, backup-restore proven.

## 10. Dev workflow & commands

- `pnpm dev` — run all apps (or per-workspace)
- `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build`
- Migrations + seed run inside `apps/api`: `pnpm prisma:migrate` / `pnpm prisma:seed`
- Conventional commits; update §9 + §14 after each completed step.

## 11. Documentation map (docs-as-code)

| File                     | Content                                                       | Maintained in   |
| ------------------------ | ------------------------------------------------------------- | --------------- |
| `README.md`              | Project overview, architecture, stack, quickstart             | 0.2 + as needed |
| `docs/setup.md`          | Dev environment, env vars, docker-compose                     | 0.3 + as needed |
| `docs/deploy.md`         | Manual VPS deploy runbook (caddy TLS, updates, rollback)      | 0.9 + as needed |
| `docs/backup-restore.md` | pg_dump + WAL/PITR, restore procedure, RTO/RPO                | 0.9 + as needed |
| `docs/api.md`            | Auto-generated OpenAPI 3 from Zod schemas (served at `/docs`) | per endpoint    |
| `docs/conventions.md`    | Code standards, i18n keys, commit style, migration workflow   | 0.2 + as needed |
| `docs/security.md`       | Encryption model, sessions/RBAC, audit, threat notes          | per change      |
| `docs/adr/`              | One file per ADR (003, 005–007, 009–011, 013) + index         | per ADR         |
| per-app `README.md`      | `api`, `admin`, `portal`, `web`, packages                     | per app         |

## 12. Git workflow

1. Work a single §9 step on a feature branch: `feat/<phase>-<slug>` (or `fix/<slug>`). **Never commit to `main`.**
2. Commit when the step is green (`feat|fix|docs|chore|refactor(...)`).
3. When a **phase or major step** completes its DoD → push the branch and open a PR; CI must be green.
4. Merge to `main` only after review; `main` stays deployable; tag `v*` for production.

## 13. ADR decision log

| #   | Date       | Decision                                                                        | Reason                                                        |
| --- | ---------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| 002 | 2026-08-14 | REST + Zod + shared `packages/contracts`                                        | Contract safety across TS apps                                |
| 003 | 2026-08-14 | Three frontends: web/admin/portal                                               | Auth isolation; web stays SEO marketing site                  |
| 005 | 2026-08-14 | MinIO object storage, proxied delivery + envelope enc.                          | Self-hosted, S3-compatible, medical documents (amended 08-15) |
| 006 | 2026-08-14 | Field-level AES-256-GCM for MedicalHistory                                      | Sensitive medical data at rest                                |
| 007 | 2026-08-14 | Basic audit log from Phase 0                                                    | No blind spot on patient-data access                          |
| 009 | 2026-08-14 | Sentry from Phase 0                                                             | Clinic-critical; fail with visibility                         |
| 010 | 2026-08-14 | pg_dump + WAL/PITR, documented RTO/RPO                                          | Medical/financial data survivability                          |
| 011 | 2026-08-14 | Manual deploy runbook initially; CD decided in 6.6                              | Ship first, automate later                                    |
| 013 | 2026-08-16 | FullCalendar 6.1.21 exact-pin for appointments calendar                         | v6 core ships no CSS/peer-deps; vendor CSS for full theming   |
| 014 | 2026-08-16 | Waiting list status-driven, no hard delete; derived no-show stats               | Status lifecycle + audit trace; no denormalized counter       |
| 015 | 2026-08-16 | Dashboard KPIs derived on read; revenue/low-stock deferred to Ph.2/3            | No invoice/stock domains yet; no placeholder data             |
| 016 | 2026-08-16 | Public web booking → PENDING waitlist entry, never an appointment               | Reuses the 1.6 contact→book lifecycle; no new table/UI        |
| 017 | 2026-08-16 | Service catalog: whole-DZD Int prices, ADMIN-only write, 2.2 snapshot           | No cents anywhere; pricing is management-sensitive            |
| 018 | 2026-08-17 | Invoices: immutable snapshot lines, atomic per-branch numbering, derived status | Void+re-issue instead of editing                              |
| 019 | 2026-08-17 | Payments & refunds: one table, derived paid amount, refunds reverse receipts    | Never edit money; paidDZD always derived                      |
| 020 | 2026-08-17 | Expenses: fixed category enum, editable but never hard-deleted (soft void)      | Audit before/after; clinical roles excluded from costs        |
|     |            | (append as decisions are made; full detail in `docs/adr/`)                      |                                                               |

## 14. Session log / progress

| Date       | Scope    | Status / Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-14 | Planning | ADRs 002/003/005–007/009–011 recorded; roadmap revised per review (walking skeleton, MinIO, encryption, audit, Sentry, backup rigor, granular scope)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-14 | 0.1      | Monorepo restructured. Web app → `apps/web/` (rename-tracked). Scaffolds: `apps/admin`, `apps/portal` (Vite React + Tailwind stubs), `apps/api` (Express + Zod + `@dentora/contracts`, tsup build, `/health` verified), `packages/contracts` (shared Zod schemas), `packages/ui`, `packages/config`. Root scripts: dev/build/typecheck/test/lint. All green: typecheck, build, lint, test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-14 | 0.2      | Tooling + CI + docs. All workspaces extend `packages/config/tsconfig.base.json`. Prettier (no-semi/single-quote) + `format`/`format:check`. GitHub Actions CI (format→lint→typecheck→test→build). Root `README.md`, `docs/` tree (setup, conventions, security, ADR index + 8 ADR files), per-app READMEs. Branch policy: work on `feat/phase0-tooling-ci-docs`; main frozen. All checks green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-14 | 0.3      | Docker infra. `infra/docker-compose.yml` (postgres+minio+api+nginx+caddy), `.env.example`, WAL archiving `on` (wal_archive volume), `.dockerignore`, `apps/api/Dockerfile` (single-stage build, `@dentora/api...` filter, bundled runtime), `infra/nginx/Dockerfile` (builds SPAs in-image) + nginx.conf (3 sites, `/api` proxy), `infra/caddy/Caddyfile` (domain routing). API routes standardized under `/api`. Verified live: postgres+minio healthy, `/api/health` End-to-end (nginx→api→contracts), all 3 SPAs served by nginx. `dangerouslyAllowAllBuilds` for pnpm 11 (esbuild/oxide postinstall). Left branch `feat/phase0-docker-infra`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-14 | 0.4      | Prisma + encryption. Phase 0.3 branch merged via commit `bf24cf2` (archived PR on `main`). Prisma 7.9 + `@prisma/adapter-pg` driver adapter (no native engine; generated client into `apps/api/src/generated`, gitignored). Schema v0: Branch, User, Session, Patient, Setting + Role enum; migration `20260814172336_init` applied to dev DB; seed creates `admin@dentora.dz`/Role ADMIN. AES-256-GCM helper (`src/lib/encryption.ts`, `ENCRYPTION_KEY` 64-hex from `.env`), 3 unit tests pass. API now exposes `/api/system/status` (db health, contract in `packages/contracts`). **Bundle fix:** ESM tsup with runtime deps external (`@prisma/client`, `@prisma/adapter-pg`, pg, express, zod, bcryptjs, dotenv `dependencies`) + Docker runtime does `--prod` filtered install of node_modules, then `node dist/index.js`; verified standalone and in-container (`api:4000/api/system/status` → `{"ok":true,"db":"up"}`). Added `.claude/.agents/.windsurf/skills-lock.json` to gitignore. All checks green: format, lint (oxlint), typecheck, tests 3/3, full `pnpm build`. Branch `feat/phase0-prisma-encryption`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-14 | 0.5      | Auth + RBAC + sessions. Migration `20260814180000_auth_sessions_token_hash` (Session stores `tokenHash` = SHA-256, never the raw token). Contracts: roleSchema, safeUserSchema, login/changePassword/updateUserRole/authResponse/userList/revokeSessions schemas. API: `src/lib/session.ts` (256-bit token gen, hash, cookie set/clear, manual cookie parse — no cookie-parser dep), `src/lib/auth.ts` (loadSession, requireAuth, requireRole, Express Request augmentation), `src/routes/auth.ts` (login/logout/me/revoke-all/change-password), `src/routes/users.ts` (ADMIN: list, `PATCH :id/role` **revokes all sessions**, `POST :id/revoke-sessions`), central error middleware. Verified live via curl: login, me, users, FORBIDDEN for DENTIST on `/api/users`, role-change → target session revoked (UNAUTHORIZED), change-password rotates + revokes others, logout. Admin app: `@dentora/contracts` dep, vite proxy `/api`→4000, pinned port 5174, login page (fr UI) + gated shell + ADMIN users panel (role dropdown + revoke buttons). Verified through the vite proxy end-to-end. Tests: session.test.ts (5) + contracts index.test.ts (4); 12 total pass. Docs: security.md (session model), api README route table, `.env.example` `SESSION_TTL_DAYS`. Branch `feat/phase0-auth-rbac`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-14 | 0.6      | Basic audit log (ADR 007). Schema: `AuditLog` model + `AuditAction`/`AuditTarget` enums (incl. PATIENT_* ready for 1.1), denormalized actorEmail, indexes (branchId+createdAt, action, actorId); migration `20260814191000_audit_log` generated via `prisma migrate diff --from-migrations` (added `SHADOW_DATABASE_URL` to prisma.config + created `dentora_shadow` in dev postgres since Prisma 7 dropped `--from-url`). `src/lib/audit.ts`: `recordAudit` + `recordAuditFor(req)` (injects actor/branch/ip/userAgent from request), `requestMeta`. Wired into auth login (success/failure), logout, change-password, revoke-all, and users role-change/revoke-sessions. `GET /api/audit` (ADMIN, branch-scoped, action/targetType/actorEmail filters, limit≤200/offset). Fixed: LOGIN_SUCCESS was being written with branchId 'system' (hidden by branch filter) → now carries the user's branch. Contracts: auditAction/auditTarget/auditEntry/auditList/auditQuery schemas. Admin `AuditPanel` (recent 50 entries table + action filter). Tests: +2 contracts audit suites (6 total in contracts). Verified live: login → LOGIN_SUCCESS, failed login → LOGIN_FAILURE, role change → USER_ROLE_CHANGE with from/to metadata, filter works. Branch `feat/phase0-audit`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-14 | 0.7      | Sentry + structured logs (ADR 009). API: `@sentry/node` 10.70 + `src/lib/sentry.ts` (`initSentry`/`captureError` no-op when `SENTRY_DSN` empty, user context + route/method extras on error), `src/lib/logger.ts` (pino JSON, `LOG_LEVEL`), request middleware logs method/url/status/durationMs, error middleware logs + captures. Admin + portal: `@sentry/react` init guarded by build-time `VITE_SENTRY_DSN` (`import.meta.env`), `<Sentry.ErrorBoundary>` around App in main.tsx. `.env.example`: `LOG_LEVEL`, `SENTRY_DSN`, `VITE_SENTRY_DSN`. Docs: setup.md Sentry section (SPA DSN baked at build), security.md incident visibility, api README. Tests: +1 sentry no-op test (15 total). Verified: api boots w/o DSN (no crash), structured request logs present, dist bundle boots, all builds green. Dropped `pino-http` (custom 6-line middleware instead). Branch `feat/phase0-sentry`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-14 | 0.8      | App shells + i18n/theme/ui. New `packages/i18n` (`@dentora/i18n`): fr/ar/en dictionaries keyed from fr (`MessageKey`), `I18nProvider` sets `documentElement.lang`/`dir` (RTL for ar), localStorage `dentora-locale`; contexts split into `src/context.ts` to keep react-refresh lint clean. `packages/ui` (`@dentora/ui`): `tokens.css` brand emerald palette + `@custom-variant dark` (Tailwind 4 class dark mode), `cn`, `Button`, `Input`, `Card`, `ToastProvider`/`useToast`, `ThemeProvider`/`useTheme` (`dentora-theme`, light/dark/system via matchMedia); contexts moved to `theme-context.ts`/`toast-context.ts` + hooks in `useTheme.ts`/`useToast.ts` (fast-refresh clean). Admin: main.tsx wires ThemeProvider>I18nProvider>ToastProvider inside Sentry.ErrorBoundary, index.css imports tailwind + `@dentora/ui/tokens.css`; App.tsx rewritten → i18n login page (brand Card, Input/Button, fr/default locale), gated shell with sidebar nav (Dashboard/Users/Audit, ADMIN-gated), lang + theme switchers, logout; views `DashboardView.tsx` (live `/api/system/status` via `api.systemStatus`, uptime, error toast), `UsersView.tsx` (role select + revoke, ROLE_KEY→msg), `AuditView.tsx` (action filter + entries, ACTION_KEY→msg). Portal: providers + tokens import + translated skeleton shell (brand header, hero, Card, lang/theme switchers). Verified: full checks green (oxlint clean incl. fast-refresh, typecheck 8/8, 15 tests, all builds), admin dev server serves on 5174 (strictPort) with `/api`→4000 proxy and workspace module resolution via `/@fs/`, tokens.css dark-variant + brand tokens compiled into dist CSS. `web` untouched. Branch `feat/phase0-shells`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-15 | 0.9      | Backups + deploy runbook (ADR 010/011). Fixed silent WAL-archiving failure: `wal_archive` volume was docker-root-owned so every `archive_command` ran as postgres user and failed (`pg_stat_archiver` `archived_count=0`, `failed_count` climbing 298→301) → `infra/postgres-entrypoint.sh` chowns `/wal_archive`; added `infra/postgres-pg_hba.conf` replication rule (hba_file) so `pg_basebackup` works. `infra/backup/backup.sh` (daily pg_dump -Z6 keep 14, WAL sync via tar-as-uid-70 off the volume, base `pg_basebackup -Ft -z -X stream` keep 3 incl. `pg_wal.tar` redo) + `restore.sh` (`--logical --yes` destructive import; `--pitr <base> [--at                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | --lsn] [--verify]`throwaway instance on 127.0.0.1:55432). CI fix merged (PR #2): ci.yml "Generate Prisma client" step + api`"prepare": "prisma generate"`. **PITR drill passed** (2026-08-15): sentinel table → base (redo `0/41000028`) → incident UPDATE+INSERT (LSN `0/42000230`) → re-sync WAL → fork to `0/41020028`→ recovered`preserve=original`, incident row absent, promoted; target-after-redo and archive-coverage gotchas hit+fixed (both went in the runbook). **RTO measured ~2 s** (small dev DB, warm disk; dominated by base extraction + replay length in real life), **RPO ≤5 min** (`archive_timeout=300`). Docs: `docs/backup-restore.md`(layout, archiving wiring, 3 restore modes, gotchas, drill checklist, off-site note) +`docs/deploy.md`(env, build/up, migrations-from-checkout, verify incl. pg_stat_archiver, rollback via image tags, incident table). 0.9 restored-state RBAC tests deliberately left to 1.0/1.1 planning (locked for 1.1: patient soft delete`archivedAt`+`PATIENT_ARCHIVED`audit event,`Patient.notes`encrypted, identity ILIKE search ADR-006-safe). Branch`feat/phase0-backups`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-15 | 1.1      | Patients CRUD + search + soft delete (search decision Option A locked). Schema: Patient.archivedAt + AuditAction gains PATIENT_ARCHIVED/PATIENT_RESTORE (dropped PATIENT_DELETE; hard delete is out for patients). Migration 20260815120000_patient_archived_at (enum recreate + column) applied via prisma migrate deploy. Contracts: genderSchema, patientInputSchema (notes max 4000), patientUpdateSchema (partial, >=1 field), patientSchema (list/summary, NO notes), patientDetailSchema (+ notes, decrypted only here), patientListSchema, patientQuerySchema (q/archived exclude                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | include                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | only/limit<=200/offset), PatientQueryParams type. API src/routes/patients.ts: router-gated requireRole(ADMIN, DENTIST, RECEPTIONIST), branch-scoped always; list = ILIKE across firstName/lastName/phone/email (identity plaintext, ADR 006-safe) with archive filter + total; POST/PATCH encrypt notes AES-256-GCM; GET :id decrypts notes + audits PATIENT_VIEW; POST :id/archive                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | restore set/clear archivedAt + audit event; empty-string->null coercion; mounted at /api/patients. Admin: PatientsView (debounced search, archived filter, table, create/edit form modal, detail modal with decrypted notes), api.ts methods, staff-role nav entry, AuditView action map updated, i18n fr/ar/en. Live-verified on internal network (throwaway container): 26 checks green - admin CRUD, notes encrypted at rest/decrypted on detail, list never emits notes, archive/restore in archived filters + audit trail, receptionist create/list OK + 403 on /users and /audit, invalid/empty 400, missing 404; test rows cleaned up. Contracts tests +4 (10 total), typecheck 8/8, oxlint clean, builds green. Dockerfile: runtime --prod now --ignore-scripts (PR-2 prepare hook broke slim image) + longer pnpm fetch retries. Branch feat/phase1-patients.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|            |          | (append every session)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-15 | UI       | Admin migrated to **stock shadcn** (new-york, neutral, as-is — no adaptation) + hand-rolled odontogram chart replaced with `react-odontogram`. **Context/decisions:** admin only (web/portal untouched); keep `ThemeProvider`/`useTheme`/`ToastProvider`/`useToast` + `@dentora/ui/tokens.css` as the infra layer (shadcn provides no theme/toast provider; no next-themes/sonner); presentational primitives only swapped; stock neutral oklch token scheme (dark emerald brand look replaced by shadcn grayscale). **Scaffold:** `apps/admin/components.json` (new-york/neutral/lucide, `@/` aliases), `src/lib/utils.ts` `cn()` = clsx+twMerge (admin already had the `@/` path alias in vite+tsconfig), shadcn `@theme inline` token block in `index.css` (kept `tokens.css` import alongside; `.dark` mirrors `ThemeProvider`). **New deps** (admin): clsx, tailwind-merge, class-variance-authority, lucide-react, @radix-ui/{slot,dialog,label,select,progress}, react-odontogram@0.5.6. **Generated `components/ui/`: button, input, card, dialog, label, select, progress (stock).** Swapped `Button/Input/Card/Modal→Dialog/Field→Label`, toolbar selects (role, locale, theme, audit filter, archive, gender) → shadcn Select, upload progress bar → Progress, doc Open/Download → Button asChild. **Odontogram:** `OdontogramChart` now wraps `react-odontogram` (SVG arch chart); maps FDI selection via `teeth-{code}` ids + `notations.fdi` (verified in dist: `id = teeth-{FDI}`, `fdi = id.replace('teeth-','')`); `teethConditions` colors teeth by status (missing/implant/crown/root fills), `theme={resolvedTheme}` follows admin dark mode, `layout=square`, `singleSelect`; **kept the per-surface ToothEditor side-panel** (status + m/d/o/b/l condition toggles → shadcn Button default/outline). Consumed-presentational surface was small & centralized (App/Users/Audit/Dashboard/DocumentsTab/PatientsView + ToothEditor). **Verified:** admin typecheck + build green (492 KB/156 KB gzip), full workspace format/lint/typecheck/test (34) green (lint shows only the canonical shadcn `buttonVariants` fast-refresh warning, exit 0); portal/web unaffected. Branch `feat/admin-shadcn`. Future: pull more shadcn components on demand.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-15 | 1.2+1.3  | Medical history + Odontogram. Migration 20260815150000_patient_clinical_records: PatientMedicalHistory/PatientOdontogram (1:1 Patient, `data` AES-256-GCM blob, plaintext `version Int` for optimistic concurrency), AuditAction += PATIENT_MEDICAL_VIEW/UPDATE + PATIENT_ODONTOGRAM_VIEW/UPDATE, AuditLog composite `@@index([branchId, action, createdAt])` for VIEW-heavy volume. Contracts: medicalHistorySchema/medicalHistoryWriteSchema (version + non-empty data)/medicalHistoryResponseSchema, odontogramSchema (FDI `##-40 prefix` keys `/^[1-4][1-8]$/`, status + per-surface condition enums), write/response variants, versionConflictSchema. Routes (patients.ts): `GET/PUT /:id/medical-history` + `GET/PUT /:id/odontogram` — shared getProtectedBlob/putProtectedBlob helpers; **upsert** (no row → create with version=1; P2002 race → falls through to locked update); **optimistic lock** via `updateMany(where {patientId, version})` counting rows; 0 matched → 409 `VERSION_CONFLICT` + current version; branch-scoped 404; invalid body 400; VIEW/UPDATE audits + composite-index-friendly. Admin: PatientDetail tab bar (Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Medical history                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Odontogram), MedicalHistoryTab (7 free-text fields, conflict-aware reload), OdontogramTab + OdontogramChart (hand-rolled SVG 4×8 FDI grid, 64×72 cells, per-surface first-condition markers, tooth `role=button`/tabIndex aria, per-tooth editor with ≥44px chips for status + 5 surfaces × 6 conditions), constants moved to odontogram.ts (fast-refresh). i18n fr/ar/en for tabs, medical fields, statuses/conditions/surfaces, 4 new audit action labels; AuditView action map extended; api client +4 methods. **Concurrency on the wire (user review):** full-blob PUT + version lock so dentist/receptionist concurrent edits surface as explicit 409 instead of silent last-write-wins; client refetches on conflict. **Verified:** contracts 16 tests (incl. first-save placeholder, FDI key bounds, stale-version 409 shape); api 9; admin+portal build; oxlint clean; format clean. **Live matrix (deployed dentora-api-1, internal network):** admin login, patient create, medical-history first-save version=1, stale PUT → 409 (error+current version), valid 2nd save version=2, GET roundtrip decrypts, odontogram placeholder null GET, first-save version=1, stale 409, valid update, GET roundtrip, encrypted-at-rest confirmed in DB (no plaintext "Metformin"/"missing" in `patient_medical_histories.data`/`patient_odontograms.data`, lengths 147/250, versions 2/2), all 4 clinical audit actions logged, receptionist reads history + 403 on `/users`/`/audit`, invalid odontogram 400, unknown patient 404; test data cleaned. Frontend docker build (nginx image) flagged container-env fragility (fixed in infra commit landed with 1.4 prereqs: `--filter '...'` recursive install). Branch `feat/phase1-clinical`. |
| 2026-08-15 | 1.4      | Patient documents: upload / list / **proxied download**. **Schema** — migration 20260815121027_patient_documents: `PatientDocument` (patientId/branchId FK, `objectKey` unique `branch/{branchId}/patient/{patientId}/{documentId}`, originalName, mimeType, **`size` = original plaintext byte length** per review note — never derived from encrypted blob, uploadedById, createdAt), `AuditAction += PATIENT_DOCUMENT_CREATE/VIEW`. **Envelope encryption (ADR 005 amended — supersedes signed URLs)** — per-object random DEK (AES-256-GCM), ciphertext = object body; DEK **wrapped under master `ENCRYPTION_KEY`**, stored as `x-amz-meta-dentora-envelope-{key,iv,tag}` on the object. `encryptDocument`/`decryptDocument` in encryption.ts; `src/lib/storage.ts` (minio dep, lazy client + bucket ensure, put/get encrypted helpers, case/strip-insensitive metadata lookup — minio JS client returns `dentora-envelope-*` without the `x-amz-meta-` prefix). **Routes** (patients.ts): `GET /:id/documents` (paginated list), `POST /:id/documents` (raw `application/octet-stream` ≤50MB, headers `X-File-Name` URI-encoded + `X-File-Mime`; orphan-object cleanup on row-create failure), `GET /:id/documents/:documentId` (**proxied download** — RBAC + branch scope, fetch → decrypt → stream with `Content-Length` = original `size`, `Content-Type` = mimeType, audits `PATIENT_DOCUMENT_VIEW`). **Contracts**: patientDocumentSchema (originalName ≤255, size ≥0), patientDocumentListSchema, patientDocumentQuerySchema (limit/offset); auditAction +2. **Tests**: 4 envelope encryption (roundtrip, wrapped-key format, tamper → throw, wrong-DEK → throw), 5 contract suites (document schemas + audit actions) → 34 total. **Admin UI**: DocumentsTab (input file → upload w/ XHR progress bar, list with formatted size/date, Open + Download links), DetailTabs +'documents'; i18n fr/ar/en (+13 doc keys, +2 audit labels); AuditView ACTION_KEY +2; api client +3 (`documents`, `uploadDocument` w/ progress, `documentUrl`). **Verified**: full suite green (lint/format/typecheck/test 34/build); deployed dentora-api-1; **live matrix (28 checks, container on dentora_internal)**: admin+receptionist login, patient create, empty list, upload 201 (name decoded, size = plaintext len, mime roundtrip), list 1, **proxied download 200 byte-exact, Content-Type + Content-Length = original 38, Content-Disposition**, receptionist download+list OK, 403 /users, 401 unauthenticated, 404 unknown doc, 400 missing name, audit CREATE+VIEW counts, and **MinIO at-rest**: object under `branch/.../patient/.../{docId}`, bytes ≠ plaintext, ciphertext length == plaintext (AES-GCM stream), 3 envelope metadata headers present (wrapped DEK in `iv.tag.ct` form), content-type preserved. Test patients `Doc Chk-*` + docs + 6 audit rows + 2 MinIO objects cleaned. Branch `feat/phase1-documents`.                         |
| 2026-08-16 | 1.5      | Appointments calendar + conflict check + statuses. **Deps**: FullCalendar 6.1.21 exact-pinned core/react/daygrid/timegrid/interaction (ADR 013). **Schema** — migration `20260816083629_appointments`: `AppointmentStatus` enum, `Appointment` (branchId RESTRICT, patientId CASCADE, dentistId? rel "AssignedDentist", createdById? rel "AppointmentCreator", status PENDING/notes-encrypted, indexes branchId+startAt, branchId+status+startAt, branchId+dentistId+startAt, patientId+startAt), AuditAction += APPOINTMENT_CREATE/UPDATE/CANCEL/RESCHEDULE/VIEW/NOSHOW. **Contracts** (+10 suites): statusSchema/TERMINAL_STATUSES, input (endAfterStart, notes≤4000), update (≥1 field, ordering), appointmentSchema (list row NO notes, names denormalized), detail (+decrypted notes), query (start/end + optional filters), conflict (`CONFLICT` + overlaps[{id,startAt,endAt,kind:'dentist'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 'patient',patientName}]). **Routes** (appointments.ts, ADMIN/DENTIST/RECEPTIONIST): `BLOCKING_STATUSES` = PENDING/CONFIRMED/COMPLETED; findConflicts checks dentist AND patient overlap `startAt<end AND endAt>start` with `status IN (...)` (CANCELLED/NOSHOW never block rebooking) + dedupe; `assertDentistInBranch` (same branch + role DENTIST else 400 UNKNOWN_DENTIST); GET / range list (notes dropped), GET /:id (decrypts notes + APPOINTMENT_VIEW audit), POST (patient-in-branch 400 UNKNOWN_PATIENT, 409 CONFLICT, 201 + CREATE audit), PATCH (404, patient/dentist revalidate, `becameTerminal` skips conflict re-check, `scheduleChanged` re-checks excluding self, actions CANCEL/NOSHOW/RESCHEDULE/UPDATE). **Bug found via live smoke**: single-field PATCH (startAt only) inverted the merged schedule — schema ordering refine only fires when both fields present → added route-level `endAt > startAt` merged-window check (400 INVALID_BODY). **i18n**: t(key, vars?) interpolation added (replaceAll `{name}`); fr/ar/en +6 audit labels + appointments block. **Admin**: api client (+4 methods + ApiError.body + parseConflict), AppointmentsView (day/week/month, drag/resize, conflict toasts via parseConflict, create/edit dialog, locale-aware), FullCalendar CSS extracted from npm tgz global bundle (v6 has no shipped .css) + shadcn-neutral themed → `appointments-calendar.css` (266 rules), App.tsx nav + CalendarDays icon + header render (canManagePatients-gated), AuditView ACTION_KEY +6. **Also this date**: admin UI-fix commit `198a583` (branch `feat/admin-ui-fixes`, unpushed) — sidebar nav labels via t(), nav icons, odontogram tooltip portaled out of DialogContent. **Verified**: full suite green (format/lint/typecheck/test 44/build), live smoke on Docker (create 201, dentist-overlap 409 kind dentist, patient-overlap 409 kind patient, adjacent slot 200, list has no notes key vs detail decrypted, cancel→rebook 200, noshow→rebook 200, reschedule-into-busy 409 dentist, free move 200, endAt-only change 200 when free / 409 when overlapping, inverted-schedule PATCH→400 after fix, non-DENTIST/bogus dentist/patient 400, list/detail row ids match); test rows cleaned, Dr. Test role reverted. Branch `feat/phase1-appointments`. |
| 2026-08-16 | 1.5 fix  | Appointments bug-fix commit `0a67a92` (branch `feat/phase1-appointments`, unpushed). View switch dead (initialView only applies on mount) → `useEffect` on viewMode calls `calendarApi.changeView(...)`; calendar scrollers dead (no height) → wrapper `h-[70vh] min-h-[480px] overflow-hidden` + `height="100%"`. Empty patient dropdown: UI requested `limit:500` but contracts cap = 200 → API 400 → list never loaded → couldn't select/save → `limit:200`. Slot selection vanished on dialog open (FullCalendar `unselectAuto` default) → `unselectAuto={false}`; dialog forced end = start+30min → create carries the full selected range (`defaultEnd`), falls back to +30 only when absent; direct creation from week/month all-day selects was dropped silently → maps allDay to 09:00–09:30. Toolbar was hardcoded English + icon-only prev/next → i18n `appointments.{today,prev,next,viewMonth,viewWeek,viewDay}` (fr/ar/en), Select trigger `w-fit`. Locale select `w-[90px]` cramped → `w-fit`. Odontogram tooltip misplaced (position:fixed against DialogContent `translate-[-50%]` + `zoom-in-95`, transform = containing block) → content centered via untransformed `fixed inset-0 grid place-items-center` wrapper, translate/zoom dropped, fade kept. **Verified**: format/lint/typecheck/test (44)/build green; live smoke `patients?limit=200` + appointments load, admin dev :5174 hot-reloaded, API dev :4000 running. Branch `feat/phase1-appointments`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-16 | 1.5 prep | **PR readiness pass** (phase closed, no push yet). (1) Fixed §9 roadmap inconsistency: 1.4 was done (log 1.4, ADR 005) but had a dangling `- [ ]` sub-bullet → proper `- [x]` checkbox. (2) Reconciled `feat/admin-ui-fixes` (`198a583`): its App.tsx nav-icons/translated-labels are already present on this branch (plus the new CalendarDays/Appointments item); its `OdontogramChart` `createPortal(document.body)` workaround is **dropped** — superseded by the transform-free `DialogContent` (`0a67a92`) and would visually relocate the chart out of the modal. Net: branch fully subsumed, safe to ignore on merge. (3) Docs: `apps/api/README.md` patients route now states `limit ≤200` (contract max — the 500→400 root cause), `docs/adr/README.md` index gained the ADR 013 row (file + §13 already present). (4) **Verified**: format/lint/typecheck 8/8/test (31+13)/build 8/8 all green. 1.5 ready to PR: commits `1056587`+`0a67a92`+`aa95c7a` (+ prep commit) → push `feat/phase1-appointments` + PR to `main` once the dev-server smoke confirms the UI fixes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-16 | 1.5 out  | Pushed `feat/phase1-appointments` to origin and opened **PR #9** (base `main`, 4 commits). Continuing on new branch `feat/phase1-waitlist` (based on `1b3a734`) so the 1.6 PR stays clean once #9 merges.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-16 | 1.6      | Waiting list + no-show handling. **Schema** — migration `20260816171558_waitlist` (folder renamed to the naming convention; `_prisma_migrations` synced): `WaitlistStatus` enum, `WaitlistEntry` (branchId, patientId CASCADE, dentistId? "WaitlistDentist" SET NULL, preferredDate?, notes?, status PENDING, appointmentId? @unique "WaitlistAppointment" SET NULL, createdById?, composite indexes branchId-first), AuditAction += WAITLIST_CREATE/UPDATE/BOOK/CANCEL. **Contracts** (+12 suites, 43 total): waitlistStatusSchema + WAITLIST_STATUSES/ACTIVE/TERMINAL, input / update (≥1 field) / list-row (no notes) / detail (+notes) / list {items,total} / query (status, dentistId, patientId, q, limit ≤200), duplicate `WAITLIST_ALREADY_ACTIVE`, patientDetail += `noShowCount`+`noShowRate` (0–1), staffDentist + list. **API**: `src/lib/noShow.ts` pure `noShowStats` (rate = noShow / [noShow+completed], 4-dec) + 5 unit tests (18 total); `routes/waitlist.ts` — list (patient-name `q` via relation OR), detail (decrypts notes), POST (patient in branch 400 UNKNOWN_PATIENT, dentist 400 UNKNOWN_DENTIST, 409 WAITLIST_ALREADY_ACTIVE on active dup, 201 + CREATE audit), PATCH (status-driven, dup-check only when becoming active, `BOOKED` requires a branch-scoped same-patient appointment else 400 BOOKED_REQUIRES_APPOINTMENT or UNKNOWN_APPOINTMENT, actions UPDATE/BOOK/CANCEL); `routes/staff.ts` `GET /dentists` (staff roles, branch-scoped DENTIST roster — fixes the dentist-dropdown 403 from the ADMIN-only `/api/users`); patients GET /:id adds derived no-show stats. **Admin**: api client +4 waitlist methods + dentists(); WaitlistView (search+status-filter toolbar, table w/ status badges, add-entry dialog patient+dentist+preferredDate+notes, Book dialog creates a real appointment via /api/appointments then links+BOOKs the entry, conflict 409 toasts via parseConflict); nav item (ListTodo, staff-gated); AppointmentsView dentist loading switched to `api.dentists()`; PatientDetail no-show line (`patients.noShowStat`, rate ×100); i18n +keys fr/ar/en incl. audit action labels; AuditView ACTION_KEY +4. **Docs**: api README (waitlist + staff-dentists routes + no-show stats), ADR 0014 + index row, §9/§13/§14 updated. **Verified**: format/lint/typecheck 8/8/test (43+18)/build 8/8; live smoke — create, duplicate 409, contact, booked-without-appointment 400, book to a real appointment (audited WAITLIST_BOOK with metadata), expire, list+filter, patient no-show fields; smoke rows cleaned. Branch `feat/phase1-waitlist`.                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-16 | 1.7      | Dashboard KPIs — **all figures derived on read, nothing stored** (ADR 015). **Contracts** (+7 suites, 49 total): `appointmentStatusCountsSchema` (exact 5 status keys), `dashboardUpcomingVisitSchema`, `dashboardVisitsSchema` (today total + byStatus + upcoming), `dashboardNoShowSchema` (today + rate30d 0–1), `dashboardWaitlistSchema` (active), `dashboardPatientsSchema` (total + new30d), `dashboardKpisSchema`, `dashboardKpisQuerySchema` (optional from/to/windowStart ISO). **API**: `routes/dashboard.ts` — GET /api/dashboard/kpis gated ADMIN/DENTIST/RECEPTIONIST; windows default to server-local today + 30d lookback; 5 parallel queries (today appointments, groupBy NOSHOW/COMPLETED 30d, active waitlist, patients total + new) → tally in `lib/dashboard.ts` (statusCounts, startOfDay, addDays, BLOCKING_VISIT_STATUSES) + 4 pure unit tests (22 total); upcoming = blocking statuses from the server clock on, sorted, capped 10 (list rows never carry decrypted notes); rate30d reuses `noShowStats` (ADR 014 formula). **Admin**: DashboardView rewritten — 4 KPI cards (visits with confirmed/pending/completed subcount, no-shows today with 30-day rate percent, active waitlist, patients with +new30d), today's schedule list (time window, patient, dentist, status badge), system-status pill + uptime + refresh button; `api.dashboard()` client method. **i18n** fr/ar/en `dashboard.*` keys (todayVisits, noShowToday, noShowRate30d, activeWaitlist, patientsTotal, newPatients30d, todaySchedule, upcomingEmpty, refresh, loadError, loadingStatus). **Docs**: api README dashboard section, ADR 0015 + index row, §13 ADR log, §14. **Verified**: format/lint/typecheck 8/8/test (49+22)/build 8/8; live smoke — KPI aggregates match query windows, upcoming caps + ordering + blocking-status filter, no-show rate math (0.6667 4-dec), waitlist active, patients counts; **regression found+fixed**: POST/PATCH /api/patients 500'd since 1.6 (patientDetailSchema requires noShowCount/noShowRate but toDetail omitted them) → shared `toDetailWithStats` (ADR 014 stats on create/update/detail); smoke rows cleaned incl. prior-session `test1 test01` leftovers (had a corrupt `endAt < startAt` appointment) and 3 duplicate `Dsh Chk1` leaked by the 500; smoke rows cleaned. Branch `feat/phase1-dashboard`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-16 | 1.8      | Public-site booking → **API creates a PENDING waitlist entry, never an appointment** (ADR 016). **Contracts** (+5 suites, 55 total): `publicBookingSchema` (firstName/lastName/phone required, service/preferredDate/message optional, phone digit-regex, msg ≤1000), `publicBookingResponseSchema` (waitlistEntryId); `waitlistSchema` += `source` (staff/web). **API**: `routes/public.ts` — POST /api/public/bookings, the only unauth route: per-IP in-memory fixed-window limiter (`lib/rateLimit.ts`, 5/h → 429), branch from `PUBLIC_BRANCH_ID` else first branch (503 if none), find-or-create patient by phone, reuse of the active-dup rule (409 WAITLIST_ALREADY_ACTIVE), entry created PENDING with encrypted notes (service + message) + `createdById = null`, audit `WAITLIST_CREATE` targeting the patient with `metadata.source: 'web'`, 201 `{waitlistEntryId}`; waitlist list/detail now select `createdById` and expose `source` (null creator ⇒ web); registered `api.use('/public')`. **Web**: BookingModal rewired — async POST, submitting/error/duplicate/already/success states (409 → distinct 'already in touch' view), name split into firstName/lastName, keeps WhatsApp secondary CTA; i18n `booking.submitting/error/already/alreadyNote` fr/ar/en; Vite dev proxy `/api` → 4000; added `@dentora/contracts` workspace dep. **Admin**: WaitlistView web badge (waitlist.fromWeb) when `source === 'web'`. **Docs**: api README public-booking section, ADR 0016 + index, §9/§13/§14, `PUBLIC_BRANCH_ID` in apps/api/.env.example. **Verified**: format/lint/typecheck 8/8/test (55+22)/build 8/8; live smoke — 201 → entry on the waitlist board as source web, duplicate same phone 409, 400 invalid body, 429 threshold, patient auto-created with the form name/phone, audit row with source web (plus patients waitlist list source mapping); smoke rows cleaned. Branch `feat/phase1-web-booking`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-16 | 2.1      | **Service catalog & pricing** (ADR 017). **Schema** — `Service` model (branchId, name, ServiceCategory enum CONSULTATION/SURGERY/CARE/HYGIENE/PROSTHETIC_ORTHO/IMAGING, `priceDZD` Int whole dinars, `durationMinutes`, `reimbursablePct` default 0, archivedAt, branchId-first indexes) migration `20260816193553_add_service_catalog`; AuditAction += SERVICE_CREATE/UPDATE/ARCHIVE/RESTORE, AuditTarget += SERVICE. **Contracts** (+5 suites, 60 total): serviceCategorySchema + SERVICE_CATEGORIES, serviceSchema (price ≥0 int, duration ≥1 ≤14400, pct 0–100), serviceInputSchema (pct default 0), serviceUpdateSchema partial, serviceListResponseSchema {items,total}, serviceQuerySchema (q≤120, category, archived exclude                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | only optional, limit ≤200 default 50). **API**: `routes/services.ts` — branch-scoped, router gated to the clinical trio, **write routes stack `requireRole('ADMIN')`** (POST/PATCH/archive/restore), list filters archivedAt by default, no per-read audit (catalog ≠ PHI) while every write audits with targetType SERVICE + metadata name/price; registered `api.use('/services')`. **Admin**: CatalogView — nav item (Tags) gated to the clinical trio with **ADMIN-only write controls** (read-only table for DENTIST/RECEPTIONIST matches the API RBAC), search + category filter + active/archived filter, price `1 500 DA` + `45 min` + coverage badge (`80%` emerald / `Non prise en charge`), create/edit modal (name/category/price/duration/coverage), archive/restore row actions; api client +5 (services/createService/updateService/archiveService/restoreService); i18n fr/ar/en `catalog.*` + nav.catalog. **Docs**: api README services section, ADR 0017 + index row, §9 (2.1 [x] + 2.2 price-snapshot clause), §13 row, §14. **Verified**: format/lint/typecheck 8/8/test (60+22)/build 8/8; live smoke — create (201, audited SERVICE_CREATE), list default excludes archived, category+q filters, PATCH price, archive→hidden, restore, 403 for a RECEPTIONIST write + 200 read as DENTIST/RECEPTIONIST, 400 invalid body/query, 404 for a foreign-branch id; smoke rows cleaned. Branch `feat/phase2-billing-21`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-17 | 2.2      | **Invoices + immutable snapshot lines + atomic numbering** (ADR 018). **Schema** — migration `20260817044250_add_invoices`: `Invoice` (branchId, patientId Restrict, `invoiceNumber` Int, issuedAt default now, `voidedAt`?, createdById?, `@@unique([branchId, invoiceNumber])`, branchId+issuedAt / branchId+patientId indexes) + `InvoiceLine` (invoiceId Cascade, serviceId? reference-only, **serviceName + priceDZD + quantity snapshots** — lines never re-read the catalog, ADR 017 clause); AuditAction += INVOICE_CREATE/INVOICE_VOID, AuditTarget += INVOICE. **Contracts** (+6 suites, 67 total): invoiceStatusSchema + INVOICE_STATUSES, invoiceLineSchema, invoiceSchema (patientName, #, status, subtotalDZD, totalDZD), invoiceDetailSchema (+lines), invoiceListSchema, invoiceQuerySchema (q, status, patientId, limit ≤200), invoiceCreateSchema (patientId + 1–50 lines: serviceId?, serviceName ≤120, priceDZD int, quantity 1–999), MAX_INVOICE_LINES. **API**: `lib/invoice.ts` — `nextInvoiceNumber` (atomic per-branch bump inside the settings `invoiceCounter` row in its own tx, unique backstop) + pure `lib/invoiceStatus.ts` `invoiceStatus` (derived status from paidDZD/subtotalDZD/voidedAt; 2.3 payments feed `paidDZD` — no stored status) + 5 unit tests (27 total); `routes/invoices.ts` — read trio+ACCOUNTANT, create/void `requireRole('ADMIN','RECEPTIONIST')`; GET / (filters), GET /:id (lines), POST / (patient in branch → 400 UNKNOWN_PATIENT, referenced services validated → 400 UNKNOWN_SERVICE, atomic number, totals derived, 201 + INVOICE_CREATE audit w/ number+patient+total), POST /:id/void (404 / 400 ALREADY_VOID, INVOICE_VOID audit); **no edit route — void + re-issue**; status filter uses voidedAt now (paid-aware join comes with 2.3); registered `api.use('/invoices')`. **Admin**: api client +4 invoice methods; InvoicesView — nav (Receipt, ADMIN/RECEPTIONIST/ACCOUNTANT) with **create/void controls only for ADMIN+RECEPTIONIST** (read-only for ACCOUNTANT, matches API RBAC), list (#000123, patient, date, derived-status badge, total) row-click → detail dialog (line items + derived totals, void button for writers), create modal (patient select + catalog quick-add lines prefilled with snapshot name/price + qty) ; AuditView labels invoiceCreate/invoiceVoid; i18n fr/ar/en `invoices.*` + nav.invoices. **Docs**: api README invoices section, ADR 0018 + index row, §9 (2.2 [x]), §13 row, §14. **Verified**: format/lint/typecheck 8/8/test (67+27)/build 8/8; live smoke — create with catalog-derived lines (201, correct sequential numbers, derived totals + UNPAID), list + q (name and number) + status=UNPAID filter, detail lines, void (400 on re-void), RBAC (RECEPTIONIST write 200, ACCOUNTANT/DENTIST write 403, read 200), 400 unknown patient/service, audit INVOICE_CREATE/INVOICE_VOID rows; smoke rows cleaned. Branch `feat/phase2-billing-22`. |
| 2026-08-17 | 2.3      | **Payments & refunds — one table, derived paid amount** (ADR 019). **Schema** — migration `20260817052640_add_payments`: `PaymentKind` (RECEIPT/REFUND) + `PaymentMethod` (CASH/CHEQUE/CARD/TRANSFER) enums, `Payment` (branchId, invoiceId Restrict, `kind` default RECEIPT, method, `amountDZD` Int, reference?, notes?, receivedAt, **`refundsId` self-relation** "RefundsPayment" for refunds reversing a receipt, createdById "PaymentCreator", invoiceId+receivedAt / branchId+receivedAt / branchId+kind indexes), AuditAction += PAYMENT_CREATE/PAYMENT_REFUND; User/Branch/Invoice relations extended. **Contracts** (+5 suites, 72 total): `paymentMethodSchema` + PAYMENT_METHODS, `paymentKindSchema`, paymentSchema (kind/method/amount/reference/notes/receivedAt/refundsId), paymentListSchema, paymentQuerySchema (invoiceId                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | invoiceNumber, limit ≤200), MAX_PAYMENT_AMOUNT_DZD, `paymentCreateSchema` (invoiceId, method, amount 1..100M, reference ≤80, notes ≤300, receivedAt optional), `refundCreateSchema`; invoiceDetailSchema += `paidDZD`/`balanceDZD`/`payments`. **API**: pure `lib/paymentMath.ts` (netPaid/refundableRemaining/balanceDue — no prisma import, CI-safe) + `lib/payments.ts` (paidForInvoices one groupBy, paymentNetFor, refundedForPayment, toPayment) +3 unit tests (30 total); `routes/payments.ts` — read trio+ACCOUNTANT, create/refund `requireRole('ADMIN','RECEPTIONIST')`; `GET /` branch-scoped ledger (invoiceId or invoiceNumber filter); `POST /` 201 receipt — no payment on a voided invoice (400 INVOICE_VOIDED), never overpays the total (400 PAYMENT_EXCEEDS_BALANCE), PAYMENT_CREATE audit (number+amount+method+balance); `POST /:id/refund` bounded by the receipt's remaining net (400 REFUND_EXCEEDS_RECEIPT), PAYMENT_REFUND audit + refundsPaymentId; registered `api.use('/payments')`. **Invoices become paid-aware**: `statusWhere` replaced — `GET /` attaches paidDZD to every row (real badges), `UNPAID                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | PARTIAL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | PAID`matched in-memory over branch candidates (single-clinic volume, ADR 019),`VOID`stays a voidedAt scan;`GET /:id`returns payments + paidDZD + balanceDZD; create/void responses include them; **void blocked while money collected** (400 INVOICE_HAS_PAYMENTS, refund first — completes ADR 018 correction flow). **Admin**: api client +3 payment methods; InvoicesView detail dialog grows a payments ledger (paid/total/balance lines, method+date+amount+reference rows, refunds shown as −reductions, Refunded tag, Encaisser modal prefilled w/ balance + method select CASH/CHEQUE/CARD/TRANSFER + reference + notes, refund modal bounded by remaining); AuditView labels paymentCreate/paymentRefund; i18n fr/ar/en`payments.*`+`invoices.paid/balance`. **Docs**: ADR 0019 + index, §9 (2.3 [x]), §13 row, §14, api README payments section + invoices section (paid-aware), ADR 0018 implementation note. **Verified**: format/lint/typecheck 8/8/test (72+30)/build 8/8; live smoke — overpay 400, full payment → PAID + balance 0, void-with-money 400, refund==receipt OK then net 0 → UNPAID → void OK, partial → PARTIAL, refund 2000 then >remaining 400, refund on zero-remaining 400, payment-on-voided 400, ledger by invoiceNumber, status filters UNPAID/PARTIAL/PAID/VOID totals exact, detail payments + paid/balance, RBAC (recep create 201 + refund 201, acc/dent create+refund 403, all read 200 — **DENTIST read initially 403 → route gate widened to trio+ACCOUNTANT to match invoice-detail exposure**), audit PAYMENT_CREATE/REFUND rows w/ metadata; smoke rows + invoiceCounter cleaned. Branch `feat/phase2-billing-23`. |
| 2026-08-17 | 2.4      | **Expenses & fixed categories** (ADR 020). **Schema** — migration `20260817081526_add_expenses`: `ExpenseCategory` enum (SALARY/RENT/SUPPLIES/EQUIPMENT/UTILITIES/MAINTENANCE/MARKETING/TAXES/OTHER) + `Expense` (branchId, category, `amountDZD` Int whole dinars, description ≤300, `incurredAt` = the date the expense belongs to — 2.5 close-out/P&L sums key off it, `voidedAt`? soft-void, createdById "ExpenseCreator", indexes branchId+incurredAt / branchId+category+incurredAt / branchId+voidedAt); AuditAction += EXPENSE_CREATE/UPDATE/VOID, AuditTarget += EXPENSE. **Contracts** (+6 suites, 78 total): `expenseCategorySchema` + EXPENSE_CATEGORIES, expenseSchema, expenseInputSchema (category/amount 1..MAX_EXPENSE_AMOUNT_DZD/description), expenseUpdateSchema (≥1 field), expenseListSchema {items,total}, expenseQuerySchema (q ≤120, category, from/to ISO, voided exclude                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | only, limit ≤200 default 50), MAX_EXPENSE_AMOUNT_DZD. **API**: pure `lib/expenseMath.ts` (expenseSums by category / expenseTotal — no prisma import, CI-safe, feeds 2.5 P&L) +2 unit tests (32 total); `routes/expenses.ts` — **ADMIN + ACCOUNTANT only, read AND write** (clinical roles excluded from costs, ADR 020); GET / (filters, non-voided by default), GET /:id, POST / 201 + EXPENSE_CREATE audit (amount+category+date+description), PATCH /:id + EXPENSE_UPDATE audit w/ before/after metadata (404 / 400 ALREADY_VOID — a voided expense is frozen), POST /:id/void soft (404 / 400 ALREADY_VOID, EXPENSE_VOID audit), **no DELETE**; registered `api.use('/expenses')`. **Admin**: api client +5 expense methods; ExpensesView — nav item (Wallet) gated ADMIN+ACCOUNTANT (both write, matches API RBAC), search + category select + active/voided + from/to filters, table (date, category badge, description, DA amount, voided rows dimmed), create/edit modal (category/amount/description/date) + row void; AuditView labels expenseCreate/update/void; i18n fr/ar/en `expenses.*` + cat labels + audit labels. **Docs**: api README expenses section, ADR 0020 + index row, §9 (2.4 [x]), §13 row, §14. **Verified**: format/lint/typecheck 8/8/test (78+32)/build 8/8; live smoke — RBAC (dentist/recep read+write 403, acc create 201 + PATCH 200 + admin create/void 201), list/q/category/from-to/voided=only filters exact, PATCH audited before/after, void then re-void 400, PATCH voided 400 ALREADY_VOID, 404 unknown id, audit EXPENSE_CREATE/UPDATE/VOID rows w/ metadata; smoke rows cleaned. Branch `feat/phase2-billing-24`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
